# Tasks: add-turbo-wild-embers-autoplay-limits

## 1. Математика wild

- [x] 1.1 Конфиг: символ `wild` (название, выплаты 60/900/20000, tier 10), веса лент [1,1,1,1,1], пресеты лимитов WIN/LOSS_LIMIT_MULTS. Verify: typecheck.
- [x] 1.2 `evaluate` с подстановкой wild («лучший кандидат»), `needsAnticipation` считает wild совпадением. Verify: selftest 13/13 — замещение, чистые wild, лучший кандидат, anticipation.
- [x] 1.3 Тюнинг RTP: выплаты младших/средних символов ×~0.75 от прежних (рычаг — выплаты, не веса: укорачивание лент увеличивало долю wild). Verify: `npm run simulate` (10M) — аналитика и Монте-Карло: **RTP 94.49%**, hit rate 20.27%.

## 2. Турбо

- [x] 2.1 ReelsView: параметр turbo (VMAX ×1.6, тайминги ×0.45, дистанция дрейфа без изменений — посадка точная); WinPresenter: turbo в opts (×0.45 к длительностям, Big Win — полная длительность); Game: состояние + persist + кнопка ⚡. Verify: сборка + smoke; визуально кнопка активна/неактивна.

## 3. Лимиты автоигры

- [x] 3.1 Game: `autoplayStartBalance`, проверка лимитов в `finishCycle` (тосты «Лимит выигрыша/проигрыша»), Ui: две кнопки-цикла в верхней панели, persist. Verify: сценарий по спецификации — стоп с объяснением при достижении лимита.

## 4. Эмберы

- [x] 4.1 `Embers.ts`: 34 частицы (blend add, подъём, синусоидальное покачивание, мерцание, ре-спавн), слой между фоном и барабанами, обновление в тикере. Verify: скриншот idle — искры видны.

## 5. Текстура wild и paytable

- [x] 5.1 Генерация текстуры wild (`wildTexture.ts`: золотая рамка, костяной череп с красными зрачками, плашка WILD) через `renderer.generateTexture`; подстановка в реестр текстур до создания видов. Verify: скриншот 06-wild-win.png — wild в ячейках.
- [x] 5.2 PaytableView: баннер wild над таблицей (иконка, выплаты, «заменяет любой символ в линии»). Verify: скриншот paytable.

## 6. Интеграция

- [x] 6.1 Build + smoke + скриншоты; `openspec validate --strict`; archive; push; CI зелёный.

## Отклонения от плана

- Первый тюнинг-подход «резать веса лент» оказался неверным: укороченные ленты повышают долю wild (q_wild = 1/L растёт) и RTP подскочил до 155%. Правильный рычаг — выплаты младших/средних символов (масштабируют и чистые, и wild-ассистированные выигрыши пропорционально).
- Аналитика в simulate.ts переписана на точный перебор кортежей с wild-подстановкой (старая формула не знала о wild и расходилась с Монте-Карло на 40 п.п.).
- Попутно найден и исправлен баг выбора тира Big Win (поиск шёл по возрастанию множителей — 6588× показывал «БОЛЬШОЙ» вместо «БОЖЕСТВЕННЫЙ»).
