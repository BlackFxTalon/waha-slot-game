# Tasks: add-grim-fortune-slot

## 1. Каркас проекта и ассеты

- [x] 1.1 Инициализировать npm-проект: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`; зависимости pixi.js@8, dev — typescript, vite. Verify: `npm run dev` открывает пустой канвас без ошибок консоли.
- [x] 1.2 Загрузчик ассетов: нарезка 9 символов из атласа (frame по сетке 3×3), спрайт фона, экран загрузки с прогрессом. Verify: в dev-версии все 9 текстур отрисовываются тестовым рядом на канвасе.
- [x] 1.3 Layout-корень 1920×1080 (scale-to-fit + letterbox), посадка фона и расчёт зоны барабанов из долей дизайна (зона измерена `tools/analyze-bg.mjs`). Verify: при изменении размера окна фон покрывает холст, сетка не выходит из тёмной центральной зоны.

## 2. Математика слота

- [x] 2.1 Конфиг игры: 9 символов (id, ячейка атласа, таблица выплат 3/4/5), 10 линий, список ставок, reel strips (компактные веса + детерминированная расшивка без соседних одинаковых). Verify: `npm run typecheck` проходит, конфиг импортируется без pixi-зависимостей.
- [x] 2.2 Модуль math.ts: `spinResult` (seedable RNG mulberry32) и `evaluate` (линии слева направо, 3/4/5, сумма выплат). Verify: `npm run selftest` — 9 проверок, включая детерминизм и независимость исхода от ставки.
- [x] 2.3 Симулятор RTP `tools/simulate.ts` (10M спинов по умолчанию): RTP, hit rate, распределение выплат. Verify: RTP = 95.02% (диапазон 92–96%), hit rate ≈ 17%.

## 3. Презентация барабанов

- [x] 3.1 ReelsView: ленты с бесшовной петлёй, маска зоны барабанов, фазы разгона/хода/торможения (2-фазная остановка: ход на полной скорости + фиксированный дрейф 8 ячеек), отскок на целевом исходе. Verify: скриншоты smoke — точная остановка на сетке из math.spinGrid (`displayedGrid() === result.grid`).
- [x] 3.2 Быстрая остановка (skip) по повторному спину/Space — множитель скорости ×3, сохраняя порядок слева направо. Verify: skip завершает прокрутку за ~1 с на заранее определённом исходе.
- [x] 3.3 Anticipation 5-го барабана при 4 одинаковых слева: продление прокрутки + пульс красной рамки + звук напряжения. Verify: юнит-тест `needsAnticipation` в selftest; визуальный триггер после остановки 4-го барабана.

## 4. Показ выигрыша и UI

- [x] 4.1 Подсветка выигрышных линий: трассы (свечение + линия + узлы), затемнение невыигравших символов, пульс выигрышных, цикличный показ линий, набегание суммы с тикающим звуком. Verify: скриншот презентации; сетка с несколькими линиями показывает их поочерёдно.
- [x] 4.2 Big Win ≥ 15× (Mega 30×, Divine 60×): полноэкранная заставка с тьером, счётом, искрами (blend add), закрытие по клику/Space/таймауту. Verify: скриншот smoke при форс-выигрыше 17.1× — заставка «БОЛЬШОЙ ВЫИГРЫШ».
- [x] 4.3 UI-панель: спин (кнопка + Space), ставка +/− из списка, баланс с плавным набеганием, окно выигрыша, тосты о нехватке средств; persist баланса/ставки/mute в localStorage. Verify: smoke — баланс восстанавливается после перезагрузки, сохраняется точно после каждого спина.
- [x] 4.4 Paytable-оверлей (символы с множителями, схема 10 линий, правила) по «i»/Escape/клику мимо. Verify: скриншот 05-paytable.png; спин заблокирован, пока открыт (клик закрывает).
- [x] 4.5 Автоигра 10/25/50/∞ со стопом по кнопке, нехватке средств и Big Win; сокращённая презентация обычных выигрышей в режиме автоигры. Verify: пресет 10 выполняет ровно 10 спинов; Big Win прерывает серию.

## 5. Звук

- [x] 5.1 audio.ts: WebAudio-синтез (вжух запуска, гул прокрутки, щелчки остановки с вариацией по барабану, riser anticipation, тик счёта, фанфары/блёстки Big Win), master gain, разблокировка по первому жесту, mute с persist. Verify: эффекты слышимы после первого клика; mute сохраняется после перезагрузки; сетевых запросов аудио нет.

## 6. Интеграционная проверка

- [x] 6.1 `npm run build` + `tools/smoke.mjs` (headless Chromium против vite preview): 3+ спина мышью и Space, skip, сверка `displayedGrid() === result.grid` через `?debug=1`, форс-выигрыш `?forceWin=1`, скриншоты 5 состояний, persist баланса. Verify: 0 ошибок консоли/страницы, баланс сходится построчно (история спинов в логе).
- [x] 6.2 Обновить tasks.md/design.md по факту, прогнать `openspec validate --strict`, заархивировать change (`openspec archive`). Verify: validate проходит без ошибок, спеки появляются в `openspec/specs/`.

## Отклонения от плана

- Ассеты переименованы в ASCII (`assets/symbols.png`, `assets/background.png`) — кириллица и пробелы в именах ломают URL-импорты.
- Добавлены dev-инструменты сверх плана: `tools/analyze-bg.mjs` (измерение тёмной зоны фона для layout), `tools/smoke.mjs` (браузерный smoke-тест на Playwright с debug-хуком `?debug=1` и форсировкой выигрыша `?forceWin=1`).
- Точная посадка сетки потребовала зеркального маппинга лент (visual = reverse(logical), vstop = L−1−stop) — задокументировано в шапке ReelsView.ts.
